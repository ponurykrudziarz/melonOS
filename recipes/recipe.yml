---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: MelonOS
description: Immutable Fedora with mangoWC'n'shieet

base-image: quay.io/fedora-ostree-desktops/base-atomic
image-version: %OS_VERSION%

modules:
  - from-file: common/sddm-modules.yml
  - from-file: common/common-modules.yml
  - from-file: common/nvidia-open-modules.yml
  - from-file: common/mangowc-modules.yml
  - type: script
    scripts:
      - setmangowcmodules.sh
  - from-file: common/final-modules.yml

  - type: signing # this sets up the proper policy & signing files for signed images to work fully
