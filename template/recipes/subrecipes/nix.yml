# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json

modules:
  - type: dnf
    install:
      packages:
        - nix
        - nix-daemon

  - type: systemd
    system:
      enabled:
        - nix-daemon
